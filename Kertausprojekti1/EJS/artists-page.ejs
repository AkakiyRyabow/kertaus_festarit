<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Festivaalit</title>
    <link rel="stylesheet" type="text/css" href="/css/header-part-styles.css">
    <link rel="stylesheet" type="text/css" href="/css/main-page-styles.css">
    <link rel="stylesheet" type="text/css" href="/css/page-common-styles.css">
    <link rel="stylesheet" type="text/css" href="/css/footer-part-styles.css">
    <script src='https://kit.fontawesome.com/4a30715f09.js' crossorigin='anonymous'></script>
</head>
<%- include("ejs-parts/header-part"); %>
    <main>
        <div class="main-content">
            <h1 class="h1-top-desc">Artistit</h1>
            <div class="top-categories">
                <% 
                if(artists)
                    for (artist of artists) { 
                        {%>
                        <div id="<%= artist.name %>" class="category-big-div">
                            <div class="category-text">
                                <h1><%= artist.name %></h1>
                            </div> 
                            <div class="category-date">
                                <p>Maa:</p>
                                <p><%= artist.country %></p>
                            </div> 
                            <div class="category-big-div-data">
                                <div class="description">
                                    <p><%= artist.desc %></p>
                                </div>
                            <div class="other-data">    
                                <div class="city">
                                    <p>Kaupungit:</p>
                                    <%for (city of artist.artist_cities) { %>
                                        <p><%= city %></p>
                                    <%} %>
                                </div>    
                                <div class="fests">
                                    <p>Festarit:</p>
                                    <%for (fest of artist.artist_festivals) { %>
                                        <p><%= fest %></p>
                                    <%} %>
                                </div>                
                            </div>                                                  
                        </div>
                    </div>
                        <%} %>
                <%} %>
        </div>
    </main>
<script  src="/js/get-data.js"></script>
<%- include("ejs-parts/footer-part"); %>